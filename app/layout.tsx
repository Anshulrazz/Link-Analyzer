import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Link Analyzer",
  description: "Link Analyzer is an intuitive and powerful web tool designed to provide a detailed analysis of any website URL. This tool checks a wide range of essential website characteristics, including validation, security, SEO metadata, content structure, and more. Whether you're a website owner, developer, or SEO specialist, Link Analyzer helps you understand the key aspects of your website and optimize it for better performance, security, and user experience.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
